
def all_elements_true(tuple_values):
    return all(tuple_values)

# Sample tuple
sample_tuple = (True, True, False, True)

# Check if all elements in the tuple are True
result = all_elements_true(sample_tuple)

if result:
    print("All elements in the tuple are True")
else:
    print("Not all elements in the tuple are True")
